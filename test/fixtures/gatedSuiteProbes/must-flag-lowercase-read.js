// A gate read in lower case. Finding U4 named this shape and U4's fix
// covered it; V3 found the two shapes below still uncovered.
const test = require('node:test');
if (process.env.runLiveThing) {
  test('costs money', () => {});
}
