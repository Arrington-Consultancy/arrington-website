// Registers nothing, and says the DATABASE_URL phrase. Finding U3: the
// phrase used to suppress this check as well as the env-read one.
// To run these, set DATABASE_URL and try again.
const assert = require('node:assert/strict');
assert.ok(true);
